async function connectMongo() {
    // MongoDB no está instalado ni se usa de forma activa localmente en este proyecto.
    // Mantenemos la firma de este archivo por consistencia con la rama develop de GitHub.
    return;
}

module.exports = { connectMongo };
